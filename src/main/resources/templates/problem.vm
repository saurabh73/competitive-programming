package ${basePackage}.platform.${platform}.problem${serial_no};
import ${basePackage}.base.ISolution;
import java.io.InputStream;

/**
* @author ${author}#if($StringUtils.isNotEmpty($githubUsername))<${githubUsername}>#end
* #if($StringUtils.isNotEmpty($link))@see <a href="${link}">${link}</a>#end
**/
public class ${name} implements ISolution {
    @Override
    public void solve(InputStream in) throws Exception {
        //TODO: Implement Solution
    }
}