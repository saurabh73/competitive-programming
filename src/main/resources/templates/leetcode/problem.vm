package ${basePackage}.leetcode.problem${serial_no};
import  ${basePackage}.ISolution;
import java.io.InputStream;

/**
*
* @author ${author}
* #if($StringUtils.isNotEmpty($link))@see <a href="${link}">${link}</a>#end ${NEWLINE}*
**/
public class ${name} implements ISolution {
    @Override
    public void solve(InputStream in) {
    //TODO: Implement Solution
    }
}